// === IMPORTS ===
// needed as without them the globals would not be recognized
import "src/bar";
import "src/lock";

// === GLOBALS ===

// used to enable logging across ags
globalThis.logging = false;
